# Projet analyse des DVF


## Initialize dependencies :
	- Create a virtual environment (her named `MLP_DVF`) in this folder with the following command : `python3 -m venv MLP_DVF`
	- Start the evironment `source MLP_DVF/bin/activate`	
	- Install dependancies: `pip install -r requirements.txt	 